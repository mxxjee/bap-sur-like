# 밥서라이크 저장소 작업 규칙

이 문서는 저장소 전체에 적용됩니다. 이후 모든 개발 작업은 아래 기준 기능과 호환성을 유지해야 합니다.

## Supabase 연동 보존 규칙

- 현재 Supabase 평가 등록 및 조회 기능은 정상 작동하는 기준 기능이다.
- 앞으로 게임 밸런스, 그래픽, UI, 이펙트, 리소스, 사운드를 수정하더라도 Supabase 연동을 제거하거나 비활성화하지 않는다.
- `feedback.js`의 평가 등록 및 조회 흐름을 임의로 삭제하지 않는다.
- `supabase-config.js`의 전역 설정 객체 이름 `BAPSUR_SUPABASE_CONFIG`를 임의로 변경하지 않는다.
- `index.html`에서 Supabase JavaScript 클라이언트가 `feedback.js`보다 먼저 로드되는 순서를 깨뜨리지 않는다.
- `feedback.js`가 `supabase-config.js`를 선택적으로 불러온 뒤 클라이언트를 초기화하는 순서를 유지한다.
- `supabase_setup.sql`의 `feedback` 테이블 이름과 기존 필드 이름을 임의로 변경하지 않는다.
- `feedback` 테이블의 기존 호환성을 유지한다.
- 평가 등록에 사용되는 `rating`, `comment`, `game_version`, `result`, `survival_time`, `final_level`, `kill_count`, `max_body_stage` 필드를 제거하지 않는다.
- 평가 목록, 평균 별점, 전체 평가 수 기능을 유지한다.
- Supabase 미연결 상태 안내와 `localStorage` 임시 저장 및 재전송 기능을 유지한다.
- 사용자 입력을 `innerHTML`로 직접 출력하지 않는다. 안전한 DOM API와 `textContent`를 사용한다.
- 보내기 버튼의 중복 전송 방지를 유지한다.
- Secret Key, `service_role` 키, 데이터베이스 비밀번호, PostgreSQL 연결 문자열 등 서버 전용 비밀정보를 클라이언트나 저장소에 추가하지 않는다.
- 공개용 Project URL과 Publishable/anon Key 값을 임의로 교체하거나 새로 생성하지 않는다.
- 추적되는 `supabase-config.js`에는 Project URL과 Publishable/anon Key만 유지하고 서버 전용 비밀정보를 추가하지 않는다.
- Supabase 관련 구조 변경이 필요하면 작업 전에 사용자에게 변경 이유와 영향을 알린다.
- 게임 기능 수정 후 평가 등록과 평가 조회에 회귀 문제가 없는지 확인한다.

## 게임 핵심 규칙 보존

- 음식 몬스터 처치 후 경험치 음식 조각이 생성된다.
- 조각을 직접 획득해야 경험치와 몸집이 증가한다.
- 몸집이 커질수록 플레이어가 느려진다.
- 레벨업 시 게임 월드가 정지한다.
- 강화 선택 후 몸집과 임시 속도 감소가 초기화된다.
- 영구 강화 효과는 유지된다.
- 모바일 세로형 웹게임 구조를 유지한다.
- GitHub Pages에서 별도 빌드 없이 실행 가능해야 한다.

## 이후 개발 작업 원칙

- 현재 Supabase 연동 기능을 유지하고, 작업 범위에 없는 Supabase 코드는 수정하지 않는다.
- 불필요한 전체 리팩터링을 하지 않는다.
- 수정 전에 영향을 받는 파일과 연동 지점을 확인한다.
- 게임 변경 후 기존 평가 등록·조회 기능에 영향이 없는지 검사한다.
- 오류가 발견되면 원인을 수정하고 결과를 보고한다.
- 작업마다 변경 파일과 실제 테스트 결과를 정리한다.
- 사용자가 요청하기 전에는 `main` 브랜치로 병합하지 않는다.
- 사용자가 요청하기 전에는 새 변경사항을 원격 저장소에 Push하지 않는다.
